/*
*Determine os resultados obtidos na avaliação das expressões lógicas seguintes,
* sabendo que A, B, C, D e E contém, respectivamente, 2, 7, 3.5, “noite” e “frio” e que existe
* uma variável lógica L cujo valor é false.
*
* a) X = (C*A && A >= B || B <= C || C != D && L || !L)|| true;
* b)A && B
* c) “dia” == D || “frio” != “clima”;
* d) D != “dia” || “frio” != “clima”;
* d) L && B / A >= C || ! A <= C;
*/

alert("Determine os resultados obtidos na avaliação das expressões lógicas seguintes,\n "+
      "sabendo que A, B, C, D e E contém, respectivamente, 2, 7, 3.5, “noite” e “frio” e que existe\n"+
      "uma variável lógica L cujo valor é false.\n"+
      "Responda de forma Logica, V ou F"
);

a = prompt("")

